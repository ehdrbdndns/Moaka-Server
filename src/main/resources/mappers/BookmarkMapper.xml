<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moaka.mapper.BookmarkMapper">
    <insert id="insertBookmarkToChunk">
        insert into chunk(link, link_title, link_description, title, description, thumbnail, section_no, regdate)
        values(#{link}, #{link_title}, #{link_description}, #{title}, #{description}, #{thumbnail}, ${section_no}, #{regdate})
    </insert>
</mapper>