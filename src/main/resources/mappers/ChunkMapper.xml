<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moaka.mapper.ChunkMapper">
    <select id="retrieveChunkBySectionNo" resultType="Chunk">
        select *
        from chunk
        where section_no = ${section_no}
    </select>
    <insert id="insertChunk">
        insert into chunk(link, title, thumbnail, section_no, regdate, description, link_title, link_description)
        values (#{link}, #{title}, #{thumbnail}, ${section_no}, #{regdate}, #{description}, #{link_title},
                #{link_description})
    </insert>
</mapper>